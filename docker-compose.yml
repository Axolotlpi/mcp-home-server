services:
  mcp-server:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - SCRIPTS_DIR=/app/scripts
      - BLACKLIST_PATH=/app/blacklist.txt
      - REPO_DIR=/app
